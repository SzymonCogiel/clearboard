    import {Container} from 'component/Layout';
    import {Button} from 'material-ui/core'
    import {Clock} from
    export default{
        title: 'Layouts/Component'
        decorators: [],
    }
    
    // ${({ clock }) => clock ? `
    //     height: 50vh;
    //     width: 50vw;
    //     ` : ''}
    // ;

    export const ClockStories: React.FC = () => (
        <Container style={{backgroundColor = }}><</Container>
    )